version: '3.8'

services:
  unimatch:
    build:
      context: .
      dockerfile: ASSIGNMENT_12/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./ASSIGNMENT_12:/app/ASSIGNMENT_12
