# Assignment 11 — backlog / GitHub Issues

Copy the titles below into **GitHub Issues** (one issue per item, or group related work).  
Close each issue when the task is done.

| ID | Title |
|----|--------|
| A11-1 | Generic `Repository[T, ID]` with full CRUD |
| A11-2 | Entity-specific repository interfaces (all entities) |
| A11-3 | In-memory dict implementations for each repository |
| A11-4 | Unit tests: in-memory CRUD, domain queries, factory |
| A11-5 | `RepositoryFactory` for `MEMORY` / `FILE` / `DATABASE` |
| A11-6 | Stubs: filesystem JSON, database, HTTP/REST (`HttpApplicationRepository`) |
| A11-7 | README: generics, Factory + DI, run instructions, class diagram |
| A11-8 | Application services with constructor DI (`application_services.py`) |
| A11-9 | Unit tests for `ApplicationService` / `UserAccountService` |

Suggested labels: `assignment-11`, `repository`, `documentation`, `tests`.

---

## Status in this repo (for your own tracking)

The items above are implemented in `ASSIGNMENT_11/` except **A11-9** (submission), which you complete when you push and post the link.
